# ==================================
# General application settings
# ==================================

# Prefix used for naming all Docker resources (containers, volumes, networks, etc.)
# Ensures consistent resource names across the stack
APP_NAME_PREFIX=iamclient4d-auth

# ==================================
# Environment configuration
# ==================================

# Primary domain or IP used for Nginx SSL certificates and reverse proxy.
# For development, use your machine's IP or hostname (e.g., 192.168.1.100, mars).
# For production, use a valid FQDN (e.g., auth.example.com).
DOMAIN_NAME=<SET_HERE_YOUR_DOMAIN_NAME_OR_IP>

# ==================================
# Docker resource naming
# ==================================

# These are automatically derived from APP_NAME_PREFIX
IAM_NETWORK_NAME=${APP_NAME_PREFIX}-network
IAM_DB_NETWORK_NAME=${APP_NAME_PREFIX}-db-network
IAM_DB_VOLUME_NAME=${APP_NAME_PREFIX}-db-data
IAM_DB_LARC_VOLUME_NAME=${APP_NAME_PREFIX}-db-log-archive

# ==================================
# Network and port configuration
# ==================================

# Port exposed to the host for the HTTPS reverse proxy (Nginx)
# Default: 443 in production; use 24443 or similar in development
GATEWAY_PORT=443

# ==================================
# Keycloak initial temporary Admin user
# ==================================

# Initial admin user credentials for Keycloak bootstrap
IAM_ADMIN_USER=tempadmin
IAM_ADMIN_PASS=12345678

# ==================================
# OCSP stapling (TLS Certificate Revocation)
# ==================================

# Self-signed certificates or CA without OCSP support — leave these commented or set to '#'
SSL_TRUSTED_CERT=#
ENABLE_SSL_STAPLING=#
ENABLE_SSL_STAPLING_VERIFY=#

# Production: uncomment and configure if using a CA with OCSP support
# SSL_TRUSTED_CERT=ssl_trusted_certificate /etc/nginx/ssl/certs/fullchain.crt;
# ENABLE_SSL_STAPLING=ssl_stapling on;
# ENABLE_SSL_STAPLING_VERIFY=ssl_stapling_verify on;